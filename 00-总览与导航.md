# 总览与导航 —— 四个 Agent 项目的源码对比分析（内核 01–10 · 启动区 11–12 · 外围区 13–17）

本文件是全系列的入口，**不分析任何一层代码**，只回答三个前置问题：这四个项目分别是什么、本系列按什么坐标系分层、以及怎么读最快。

> 本系列对 **4 个开源 Agent 项目**（pi / deepseek-harness / codex / Claude-Code）做「同一架构层次、不同实现」的代码级横向对比，**每章一层**。
>
> 全文按**三区**组织：**启动区**（循环之前）、**内核区**（一轮循环及其子系统）、**外围区**（内核之外）。
>
> **章编号与层次槽位是两套坐标**：章号（01、02、…）只表示撰写批次，层次槽位（`L0.1` / `L1`–`L15`）才表示架构位置。内核区已有先例——第 10 章先于第 9 章写成。已完成与规划中的槽位见第四节。
>
> 追加这批章的原因：内核十章把「一轮循环」拆得很细，但**循环之外还包着两层**——启动区决定它开起来是什么样、发给模型的第一坨怎么拼，外围区决定它怎么被别的程序驱动、怎么被看见。这两层此前只在其他章里被当作黑盒引用。

---

## 一、四项目档案卡

| 项目 | 源码路径 | 语言 / 运行时 | 规模 | 架构一句话 | 源码可靠性 |
|---|---|---|---|---|---|
| **pi** | `codes/pi` | TypeScript / Node | 多包 monorepo | 事件流驱动的轻量双层主循环，会话为树 + 分支游标 | 官方源码，完整 |
| **deepseek-harness** | `codes/deepseek-harness` | TypeScript / Node | 多包 monorepo（分层最细） | 事件溯源：append-only 会话日志为唯一事实源，循环是日志之上的派生状态机 | 官方源码，完整 |
| **codex** | `codes/codex` | Rust (tokio) | 多 crate workspace | 全 async 运行时，工具并发队列 + Responses API 原生 item 直出，压缩内建为循环一部分 | 官方源码，完整 |
| **Claude-Code** | `codes/Claude-Code` | TypeScript / Bun | 单包，1987 个 TS 文件 | provider 原生消息流 + 包裹式压缩（自动/反应式/微压缩/记忆压缩多层） | ⚠️ **由 source map 还原，含 `feature()` 编译门控**，见第六节 |

> Claude-Code 的版本号为 `999.0.0-restored`，仓库自述为「从 npm 包 source map 还原的完整 TypeScript 源码，可本地运行」。因此它的结论需要标注证据等级（第六节），不能与前三者的「官方源码即生产行为」等同看待。

---

## 二、模块地图：架构层次 × 四项目入口

```text
索引层  00 总览与导航 —— 提供坐标与规范
  │
  ├─ 启动区（循环之前）
  │    ├─ L0.1 第 11 章  配置 · 凭据与启动引导   它开起来是什么样
  │    └─ L0.2 第 12 章  提示词与上下文注入      发给模型的第一坨怎么拼
  │
  ├─ 内核区（一轮循环及其子系统）
  │    ├─ L1   第 1 章   主循环                  谁在驱动「模型 → 工具 → 决策」
  │    ├─ L2   第 2 章   工具调用                tool_use → tool_result 的调度与保序 · 进程与 Shell 执行
  │    ├─ L3   第 3 章   工具定义                工具如何被描述、投影、准入 · 程序化调用
  │    ├─ L4   第 4 章   上下文压缩              窗口装不下时怎么办
  │    ├─ L5   第 5 章   消息 · Session          一段对话的事实源是什么   ★ 根源层
  │    ├─ L6   第 6 章   持久化与恢复            事实源怎么落盘、怎么读回
  │    ├─ L7   第 7 章   任务与子 Agent          把工作交给另一个 Agent · 对等协作
  │    ├─ L8   第 8 章   权限与沙箱              谁有权说「这条命令可以跑」
  │    ├─ L9   第 9 章   模型调用与 Provider     怎么把 N 种线协议归一成一套事件词汇表
  │    └─ L10  第 10 章  Hook 与扩展机制         别人能让它做什么
  │
  └─ 外围区（内核之外）
       ├─ L11  第 13 章  MCP 与外部能力接入      能力从哪来
       ├─ L12  第 14 章  长时任务与后台作业      循环结束之后还在跑
       ├─ L13  第 15 章  对外接口与集成协议      谁在驱动它
       ├─ L14  第 16 章  交互层与终端 UI 机制    人怎么看见它
       └─ L15  第 17 章  遥测 · 追踪 · 诊断      贯穿所有层
```

**图 0-1**：全系列三区结构。**启动区**在循环之外但决定循环的取值与输入；**内核区**是原有十章，L5 是其余各层设计分歧的根源层，L9 的位置特殊——它是唯一「其抽象宽度反过来框住 L1 形状」的一层（词汇表越窄，上层循环越瘦），L10 则唯一是**外部代码反向伸进运行时**的一层；**外围区**包住内核，按「离内核的距离由近及远」排列——能力入口（L11）→ 执行外延（L12）→ 驱动面（L13）→ 呈现面（L14）→ 贯穿观测（L15）。

每一「篇」对应一个架构层次。下表给出该层在四个项目里的**代码入口**——这是本系列「同一层次对比」的坐标系。**状态**列的「已完成」指该章已产出，「规划」指槽位已定、章未写。

| # | 层次槽位 | 架构层次 | pi | deepseek-harness | codex | Claude-Code | 状态 |
|---|---|---|---|---|---|---|---|
| 11 | L0.1 | **配置 · 凭据与启动引导** | `coding-agent/src/config.ts` + `core/settings-manager.ts` + `packages/ai/src/auth/*` | `packages/settings/*` + `packages/boot/*` + `packages/credentials/*` | `codex-rs/config/*` + `codex-rs/login/*` | `src/utils/settings/*` + `src/cli/*` + `src/utils/auth.ts` | 已完成 |
| 12 | L0.2 | **提示词与上下文注入** | `coding-agent/src/core/system-prompt.ts` + `core/resource-loader.ts` | `packages/core/system-prompt/src/index.ts` + `packages/context/agent-instructions/*` | `core/src/context/world_state/*` + `core/src/agents_md.rs` | `src/constants/prompts.ts` + `src/utils/claudemd.ts` | 已完成 |
| 01 | L1 | **主循环** | `packages/agent/src/agent-loop.ts`（`runLoop`） | `core/agent-loop/src/agent.ts`（`ReactLoopAgent`） | `core/src/session/turn.rs`（`run_turn`） | `src/query.ts`（`query()`）+ `src/QueryEngine.ts`（`submitMessage()`） | 已完成 |
| 02 | L2 | **工具调用调度** | `agent-loop.ts:505`（`executeToolCalls`） | `core/agent-loop/src/tool-calls.ts` | `core/src/tools/{parallel,router}.rs` | `src/Tool.ts` + `src/tools.ts` | 已完成 |
| 03 | L3 | **工具定义机制** | `packages/agent/src/harness/tools/*` | `core/tools/src/{schema,types,index}.ts` | `tools/src/tool_spec.rs` + `core/src/tools/spec_plan.rs` | `src/Tool.ts` + `src/tools/<Name>Tool/` | 已完成 |
| 04 | L4 | **上下文与压缩** | `harness/compaction/*` | `packages/compaction/compaction-*` | `core/src/compact*.rs` | `src/services/compact/*` | 已完成 |
| 05 | L5 | **消息 / 事件 / Session** | `harness/session/types.ts` + `ai/src/types.ts` | `core/session/src/{types,surface}.ts` + `llm/llm/src/message.ts` | `protocol/src/{items,models}.rs` + `core/src/context_manager/` | `src/types/message.ts` + `src/utils/{messages,sessionStorage}.ts` | 已完成 |
| 06 | L6 | **会话持久化与恢复** | `harness/session/jsonl/*` | `packages/session/session-persistence-jsonl/*` | `codex-rs/rollout/*` + `codex-rs/history/*` | `src/utils/{sessionStoragePortable,listSessionsImpl}.ts` | 已完成 |
| 07 | L7 | **任务与子 Agent** | 扩展层 spawn 独立进程 | `packages/subagent/subagent/*` | `core/src/agent/*` + `core/src/tools/handlers/multi_agents*.rs` | `src/tasks/*` + `src/Task.ts` | 已完成 |
| 08 | L8 | **权限与沙箱** | 无内建层（`beforeToolCall` 钩子 + 扩展） | `packages/sandbox/{sandbox,sandbox-local,sandbox-policy,sandbox-windows-acl}` | `codex-rs/{sandboxing,execpolicy,linux-sandbox}` + `core/src/tools/{orchestrator,approvals,sandboxing}.rs` | `src/utils/permissions/*` + `src/hooks/toolPermission/*` + `src/utils/sandbox/*` | 已完成 |
| 09 | L9 | **模型调用与 Provider** | `packages/ai/*`（41 provider + 20 api 模块） | `packages/llm/*`（llm / llm-deepseek / llm-pi-ai / llm-retry / token-meter） | `codex-rs/{codex-api,model-provider,model-provider-info,models-manager}` + `core/src/client.rs` | `src/services/api/*` + `src/utils/model/*` | 已完成 |
| 10 | L10 | **Hook 与扩展机制** | `coding-agent/src/core/extensions/*` | `packages/hooks/*` + `packages/extensions/*` | `codex-rs/hooks/*` + `codex-rs/ext/extension-api/*` | `src/utils/hooks.ts` + `src/utils/plugins/*` + `src/skills/*` | 已完成 |
| 13 | L11 | **MCP 与外部能力接入** | 扩展层（无独立包） | `packages/mcp/*` | `codex-rs/rmcp-client/*` + `codex-rs/codex-mcp/*` | `src/services/mcp/*` + `src/components/mcp/*` | 已完成 |
| 14 | L12 | **长时任务与后台作业** | `packages/agent/src/harness/pico3/*`（内建 kind `pi.job` 与内核调度器）+ `packages/durable/*` | `packages/jobs/*` + `packages/schedule/*` + `packages/goal/*` | `codex-rs/exec-server/*` + `codex-rs/core/src/unified_exec/*` | `src/tasks/*` + `src/utils/task/*` + `src/tools/ScheduleCronTool/*` | 已完成 |
| 15 | L13 | **对外接口与集成协议** | `packages/{server,protocol,client}/*` + `coding-agent/src/modes/rpc/*` | `packages/acp/*` + `packages/sdk/*` + `packages/host/webserver/*` + `packages/api/gateway/*` + `packages/webhook/*` | `codex-rs/app-server*/*` | `src/bridge/*` + `src/cli/{structuredIO,print,transports}/*` + `src/entrypoints/{sdk,mcp.ts}/*` | 已完成 |
| 16 | L14 | **交互层与终端 UI 机制** | `packages/tui/src/*` + `packages/coding-agent/src/modes/*` | `packages/client/{ui-renderer,ui-slots,ui-chat,ui-conversation,ui-tool}/*` + `packages/api/session-controller/src/client/*` | `codex-rs/tui/*` + `codex-rs/terminal-detection/*` | `src/ink/*` + `src/screens/REPL.tsx` + `src/hooks/{useCancelRequest,useExitOnCtrlCD,useVimInput}.ts` | 已完成 |
| 17 | L15 | **遥测 · 追踪 · 诊断** | `packages/telemetry/*` + `core/bug-report.ts` | `packages/runtime-diagnostics/*` + `packages/feedback/*` | `codex-rs/{analytics,otel,rollout-trace}/*` | `src/utils/telemetry/*` + `src/services/analytics/*` | 规划 |

---

## 三、核心术语对照表

**读法**：同一行是「同一个概念」，四列是它在各项目中的**原名**。跨篇阅读时先回这张表，能消掉大部分理解成本。

| 概念 | pi | deepseek-harness | codex | Claude-Code |
|---|---|---|---|---|
| 一轮「模型 → 工具 → 决策」 | `turn` / `step` | `turn` / `step` | `turn` / sampling request | query 迭代 / `turn` |
| 会话事实源 | `Session`（树：`Entry` + `Branch`） | `Session` 日志（`SessionEvent[]`）+ `surface` 投影 | Thread history（`ResponseItem` 序列） | `Message[]`（`uuid` + `parentUuid` 链） |
| 消息单元 | `AgentMessage`（content blocks） | `Message`（含 `source` + `ContextForm`） | `ResponseItem` / `TurnItem` | `Message`（`type` 标签联合，9 类） |
| 工具定义契约 | `ToolDefinition` + 三层包装 | `ToolDefinition`（强制 `output` 契约） | `ToolExecutor` trait + `ToolSpec` | `Tool.ts` 接口 + 各 Tool 目录 |
| 压缩产物 | compaction entry（含 `retainedTail`） | `compaction/*` 事件 + checkpoint | `CompactedItem` / `ContextCompaction` | `isCompactSummary` 消息 + 边界系统消息 |
| 失效 / 删除标记 | —（树不可变） | `shadowedRange` / `shadowedSeqs` | —（重写 history） | `TombstoneMessage` |
| 分支 / 分叉 | `Branch`（fork + `branch_summary`） | —（线性日志） | Forked history | `parentUuid` 链 |
| 扩展点 | 回调（`prepareNextTurn` 等）+ extensions | 声明合并 + 命名瀑布钩子 | `extension-api` + hooks | hooks（生命周期）+ plugins + skills |
| 持久化载体 | JSONL + `tmp/rename` 原子替换 | JSONL + `link()` no-clobber 代际 | rollout JSONL（不可变 + revert） | sessionStorage（便携 JSON 存储） |
| 取消语义 | `AbortSignal` + 保留 partial | 分类取消（user/parent/hook/disposed） | `CancellationToken` + `TurnAborted` | `useCancelRequest` + 打断消息 |
| 子 Agent 实体 | 独立进程（扩展 spawn） | `SubagentRun`（可被驱动的会话） | Thread（线程图节点） | `Task`（一等任务对象） |
| 子 Agent 深度限制 | 未设限 | `maxDepth` 默认 1 | thread spawn 上限 | 非内部构建禁用 |
| 权限判定入口 | `beforeToolCall`（可 block） | `sandboxPolicy.resolve()` | 审批要求构造 → 沙箱选择 | 判定链 `hasPermissionsToUseTool` |
| 沙箱模式 | —（外包给容器） | `SandboxMode`（3 值） | `SandboxPolicy`（4 值） | 并入 `PermissionMode` |
| 规则 / 策略载体 | 扩展代码 | 部署配置 + 预设表 | `.rules`（Starlark DSL） | settings 文件（8 类来源） |
| 系统隔离实现 | —（README 声明不做） | runner 链（bwrap→landlock / seatbelt / ACL） | Seatbelt / Landlock / 受限令牌 | 委托 `@anthropic-ai/sandbox-runtime` |
| 扩展单位 | 进程内模块（jiti） | Cordis 插件 | hooks（进程外）+ contributor（进程内） | 六层（hook / plugin / skill / command / MCP / subagent） |
| 挂载点数量 | 11 内核回调 + 36 事件 | 7 拦截点（5 种调度模式） | 12 hook 事件 + 12 类 contributor | 27 hook 事件 |
| 阻断信号 | `{block:true}` | 决策枚举 `deny` | JSON `decision:block` | **`exit code 2`** |
| provider 标识 | `ProviderId`（41 项已知，`ai/src/types.ts:35`） | 路由键字符串（`llm/src/types.ts:224`） | `ModelProviderInfo`（20 字段配置） | `APIProvider` 四值枚举（`model/providers.ts:4`） |
| 模型标识 | `Model<TApi>` 对象，自带 cost / contextWindow | `(provider, id)` 二元组 | `ModelInfo`（`models-manager`） | `ModelName: string`（无结构） |
| 模型清单来源 | 构建期生成（41 个 shard） | adapter `listModels()` / 模型发现 | 远端 `/models` + 磁盘缓存（TTL 300s） | 硬编码 4 provider × 11 模型 ID 矩阵 |
| 统一事件词汇表 | `AssistantMessageEvent`（**12 种**） | `StreamChunk`（**7 种**） | `ResponseEvent`（**17 种**） | **无**（原生 `stream_event` 直通） |
| 归一化边界 | 适配器内部 | 契约层 | SSE 解析层 | 调用方 |
| 线协议数 | 10 个 `KnownApi` | 由 adapter 决定 | **1**（`WireApi::Responses`） | 1（Anthropic Messages） |
| 请求级重试默认 | **0**（harness 层 3 次） | 5 | 4（请求）/ 5（流） | 10（529 另限 3） |
| usage 是否计价 | 是（含 1h 缓存写 2x） | **否** | **否** | 是 |
| 计量累计方式 | 内存累加 | **从 durable 日志回放** | 内存累加 | 内存累加 |
| 系统提示词的分节载体 | `SystemMessage.sections`（具名映射） | `PromptSection` + `order` 表 | `WorldStateSection`（`IndexMap` 键控） | 数组元素 + 段注册表 |
| 分节顺序的决定方式 | 代码书写顺序 | 显式整数 `order` | `add_section` 调用序 | 数组字面量顺序 |
| 分节是否自定界 | 是（同名标签包裹） | 否 | 否 | 否 |
| 项目指令候选名 | `AGENTS.override.md` → `AGENTS.md` → `CLAUDE.md` | 由 `agent-instructions` 配置 | `AGENTS.md`（含 fallback） | `CLAUDE.md` 系列 + `@include` |
| 项目指令超限动作 | 无上限 | 整份忽略 / 字节截断 | 字节截断 + warn | 仅告警不截断 |
| 分节重算粒度 | 每轮 turn | 每 step | 每 step | 每次 `ask()` |
| 增量提交方式 | 只发变化的节（补丁消息） | `append` / `replace` 投影 | `render_diff` | 段级 memo 复用 |
| 缓存边界标记 | 无（靠分节名稳定） | 无（靠 `order` 稳定） | 无（靠 section ID 稳定） | **`SYSTEM_PROMPT_DYNAMIC_BOUNDARY`** |
| 环境探测项数 | 1（cwd） | 4（时间 / tmux / 审批 / 沙箱根） | 7（cwd / shell / 日期 / 时区 / 网络 / 文件系统 / 子代理） | 8（cwd / worktree / git / 平台 / shell / OS / 模型 / 知识截断） |
| 配置层级数 | 2（全局 / 项目） | 4（patch 列表） | 10（含 `cwd` / `tree` / `repo` 三个项目级变体） | 5（user / project / local / flag / policy） |
| 优先级的表达方式 | 调用顺序（无优先级表） | 数组下标 | **显式数值序** `precedence()` | 数组下标 + 层内特判 |
| 配置文件载体 | `settings.json` + `models.json` + `auth.json` | 插件条目 + `cordis.patch.yml` | `config.toml` | `.claude/settings.json` + `managed-settings.json` + `managed-settings.d/` |
| schema 技术与未知键 | `settings.json` 无校验（仅 `models.json` 用 typebox）；未知键保留 | Schemastery（插件自带）；校验后显式 merge 回去 | serde + schemars；**拒绝**（`deny_unknown_fields`） | zod；保留（`.passthrough()`） |
| 企业托管配置的位置 | — | `$DSH_HOME/cordis.patch.yml` | `EnterpriseManaged`（15 分）与 legacy 托管（40 / 50） | `policySettings`（排链尾因而胜出） |
| 凭据解析顺序 | override → 存储 → ambient | env → file → dotenv | 四档（含 keyring 回退） | env → FD → helper → claude.ai |
| 平台安全存储与文件回退 | 无密钥库；`auth.json` `0600` | 无密钥库；`.credentials.yaml` `0600` | keyring；`auth.json` `0o600`（模式 Auto / Keyring / File 可配） | keychain；`.credentials.json` `0600` |
| 配置演化机制 | 迁移函数集 + 旧文件改名 `.migrated` | 文档 `version` 字段（不匹配即拒绝） | 键别名表（无全局版本号） | `migrationVersion`（当前 11，整批重跑） |
| 项目信任与未信任时挡什么 | `ProjectTrustStore`（设置管理器创建两次，非交互直接拒） | **无**（不读项目级配置，故无需设门） | `projects` 映射 + 三态 `TrustLevel`；挡 12 项 denylist | 全局配置 latch；`env` 降到 `SAFE_ENV_VARS` 白名单 |
| 「启动完成」的判据 | 主函数返回 | **7 项必需条目清单**（启用了就必须激活） | 一串步骤依次返回 | `preAction` 钩子返回 |
| MCP 接入形态 | 无（文档化拒绝，`README.md:537`） | Cordis 插件，一实例一 server | 两个 crate（`rmcp-client` + `codex-mcp`） | 单一 `services/mcp` 服务层 |
| MCP 传输类型 | 无 | 2（stdio / streamable-http） | 6（Legacy 与 2026 两种 stdio、in-process、executor、事件流、HTTP） | 8（另含 `ws-ide` 与 `claudeai-proxy`） |
| MCP 命名空间 | 无 | `mcp__<server>__<tool>`，上限 64 | 同形，上限 128 | 同形，不设名字上限 |
| MCP 调用归口 | 无 | 统一工具注册表 | 工具表 + 一等审批动作 `McpToolCall` | 统一规则匹配（靠 `mcpInfo`） |
| MCP 凭据机制 | 无 | 静态 `env` / `headers` | keyring + OAuth + 企业 EMA | SecureStorage + OAuth + XAA |
| MCP 协议版本策略 | 无 | 交 SDK 自动协商 | 显式两态 `McpProtocolMode` | 不声明，交 SDK 默认 |
| 「活过一轮」的实体 | `Task`（内建 kind `pi.job`） | `TrackedJob`（服务 `ctx.jobs`） | `ProcessEntry` | `TaskState`（7 型联合） |
| 作业登记表 | `Session.liveTasks`（内存 Map）+ JSONL | `JobRegistry.store`（内存 Map） | `Mutex<HashMap<ProcessId, …>>` | `AppState.tasks`（React store） |
| 作业输出载体 | 作业自身 live slot | 每作业一个内存环 | exec-server `VecDeque` + 内核 `HeadTailBuffer` | 每任务一个磁盘文件 |
| 输出消费游标 | `api.slot` 视图 | `read`（动 cursor）/ `readAt`（不动） | `process/read(after_seq…)` | `getTaskOutputDelta(fromOffset)` |
| 结算回注方式 | `pi.notice`（强制恰一条 user 消息） | `owner.followup`（空闲）/ `owner.inject`（忙） | 纯工具返回 + `ExecCommandEnd` | `<task-notification>` attachment |
| 唤醒预算 | — | 默认 3（`maxConsecutiveWakes`） | — | — |
| 周期触发规则 | `notBefore` / `every` | `after` / `at` / `every`（≥ 300 秒） | 无（仅一次性 `clock.sleep`） | 5 段本地时区 cron |
| 同目录双开的互斥 | — | — | — | `O_EXCL` 租约锁 |
| 跨进程重启的让位点 | 取决于注入的 `ProcessHost` | 作业否，定时靠会话事件 | `attach` 重挂（30 秒窗口） | 仅 `RemoteAgentTask` 有 sidecar |
| 取消的三段式 | 标记 → 信号 → join（标记先落盘） | cancel → 置 `stopping` | `termination_requested` → `terminate` | 查表 → 校验 running → `kill` |
| 对外驱动面的套数 | **2**（Unix socket service-call + stdio NDJSON，共享字段为零） | **4**（ACP / SDK / Web / Webhook） | **1 协议 + 4 承载**（同一份协议可换承载） | **3**（`--print` 管道 / 远程桥 / 反向当 MCP server） |
| 对外协议的信封形态 | service-call：`{serviceId, member, args}` | 各面自定（ACP 语义 / SDK JSON-RPC / Web 流协议） | 自有 RPC：`{id, method, params}`，**声明不写 `jsonrpc` 字段** | `type: control_request` / `control_response` |
| 协议宽度 | 主栈 7 服务 **24** 成员 / headless **33** 命令 | ACP **8** + SDK **3** + Web **6** 命名空间 | **170** 条 `ClientRequest` / **84** 条 `ServerNotification` | 上行 **21** 控制子类型 / 下行 **24** 种消息 |
| 分帧与编码 | **4 字节大端长度前缀 + CBOR** | 换行切分 NDJSON / WebSocket 帧 | NDJSON（stdio/UDS）/ WebSocket 一消息一帧 | NDJSON（`ndjsonSafeStringify` + `\n`） |
| 握手与协议版本 | `hello` 两帧，**只接受等值版本** | ACP 有版本字段 + 完整能力协商 | `initialize` / `initialized` + capabilities，**协议里无版本号** | `initialize` 只置一个布尔量，无能力协商 |
| 会话镜像载体 | `HostedSession.attachments`（附着集合） | ACP / SDK 各自 `sessions` 映射 | `ThreadStateManager` 三张表 | `activeSessions` 映射（远程侧） |
| 一条会话能否被多客户端同时接 | 能（写由 lane 串行化，冲突回 `lane_busy`） | **ACP 明确拒绝**第二持有者 / Web 无写者归属 | 能（反向请求**广播**、**首个响应者生效**） | 只有远程**只读**订阅（本地无 attach） |
| 服务端反向请求（审批回流） | **无**（协议层从类型上不存在） | ACP `session/request_permission` | **11** 条 `ServerRequest`（含 5 条审批） | 1 条 `can_use_tool` |
| 审批档位与「记住多久」归谁 | —（无审批） | 2 档，判给**「永不记住」** | 6 档，判给**客户端**（服务端只翻译不缓存） | 2 档，判给**宿主**（回传 `updatedPermissions`） |
| 进门凭证 | **全无**（socket `0o600` + 目录属主） | 按承载分流：Web 有 Cookie(HMAC) + token，ACP / SDK 无 | 按承载分流：**非 loopback 无凭证直接拒绝启动** | OAuth + **设备令牌**（本地只解码不验签） |
| 序号与重放所在层次 | 会话状态复制层（chord op 序号） | 会话快照游标（`snapshot.cursor`） | **不解**（协议层无序号） | **传输层**（`from_sequence_num` / `Last-Event-ID`） |
| 孤儿 / 僵尸回收 | kind 未注册 → `orphaned` | teardown 抛错 → force-fail | 退出后保留 30 秒再移除 | 终态 + `notified` → eviction |
| 渲染的基本单位 | 组件树的 `render(width)` 返回**行数组**（`tui.ts:952` 触发的 `doRender`） | React 元素树（`ui-renderer/src/client/app.tsx:21`） | `Buffer` 单元矩阵，双缓冲互换（`custom_terminal.rs:141` / `:460`） | `Screen` 单元数组（`screen.ts` 池化 Cell/Style/Char） |
| 「一帧」的触发 | `requestRender()` → `nextTick` → 16ms 定时器（`tui.ts:952-1004`） | store 变更通知 + 发布节奏声明（`store/src/index.ts:72-79`） | `FrameRequester::schedule_frame()` 合并后广播（`tui/frame_requester.rs:96`） | `throttle(onRender, 16)`（`ink.tsx:213`） |
| 差量的比对对象 | 上一帧行数组 vs 新行数组（`tui-main-screen.ts:366-370`） | 无自建差量，交给虚拟 DOM | 前后 `Buffer` 单元 diff（`custom_terminal.rs:444`） | 前后 `Screen` 单元 diff（`log-update.ts:123`） |
| 增量的「可提交」判据 | 无独立判据，每个增量都重建组件（`assistant-message.ts:91-117`） | 每种事件源的发布节奏声明（`assistant.ts:316-321`） | markdown 流在**换行边界**提交（`markdown_stream.rs`） | 只显示到**最后一个换行**（`REPL.tsx:1505`） |
| 已完成内容的归宿 | 留在组件树里，随视口滚动 | 留在 DOM 里，语义锚点补偿滚动（`use-chat-viewport.ts:367-377`） | 写进**终端原生 scrollback**（`insert_history.rs:204-216`） | 留在 Screen 与终端屏里 |
| 工具调用的状态载体 | 组件字段 `executionStarted / argsComplete / isPartial / isError`（`tool-execution.ts:188-212`） | 由 block 形状推导的纯函数（`tool-call-model.ts:239-243`） | `ExecCell` 的字段推断（`exec_cell/model.rs:67`） | REPL 的 `inProgressToolUseIDs` 集合（`REPL.tsx:1417`） |
| 「被中断」的来源 | 组件与状态位同步推进（`interactive-mode.ts:3482-3519`） | **前端合成**，后端不显式给（`tool.ts:191-205`） | 由 `TurnStatus::Interrupted` 通知回收（`chatwidget/protocol.rs:464`） | 事件流写入 `inProgressToolUseIDs`（`hooks/useRemoteSession.ts:250-297`） |
| 中断键位 | Esc（上下文相关）+ Ctrl+C 两按 + Ctrl+D（`interactive-mode.ts:2965-2991`、`:4116-4129`） | 无键位，输入条上的 Stop 按钮（`InputBar.tsx:302-316`） | Esc / Ctrl+C（`keymap.rs:1662`），双按退出被关闭（`bottom_pane/mod.rs:233`） | `escape` / `ctrl+c` / `ctrl+d` 三键分工（`defaultBindings.ts:40-41`） |
| 界面内容的权威源 | 会话存储，可从 `buildContextEntries()` 整体重建（`interactive-mode.ts:4107-4110`） | Host 会话日志 + 快照，resync 重建窗口（`sessions/session.ts:473-485`） | app-server 为权威，完成事件反校流式结果（`chatwidget/streaming.rs:194-195`） | REPL 本地消息数组，启动时接受一次注入（`REPL.tsx:1214`） |

> 空白格表示该概念在对应项目中**没有对应物**——这本身就是一条重要结论（例如 dsh 没有分支、pi 没有 tombstone）。

---

## 四、报告索引

**当前版本（四项目深度版，7 段骨架）**

| 编号 | 主题 | 文件名 | 行数 | 图 | 层次槽位 | 状态 |
|---|---|---|---|---|---|---|
| — | **总览与导航**（本文） | `00-总览与导航.md` | 363 | 1 | 索引层 | 已完成 |
| 11 | 配置 · 凭据与启动引导 | `11-配置凭据与启动引导四项目对比分析.md` | 2939 | 3 | L0.1 | 已完成 |
| 12 | 提示词与上下文注入 | `12-提示词与上下文注入四项目对比分析.md` | 1217 | 3 | L0.2 | 已完成 |
| 01 | Agent 主循环 | `01-Agent主循环四项目对比分析.md` | 698 | 2 | L1 | 已完成 |
| 02 | 工具调用流程 | `02-工具调用流程四项目对比分析.md` | 785 | 2 | L2 | 已完成 |
| 03 | 工具定义机制 | `03-工具定义机制四项目对比分析.md` | 796 | 3 | L3 | 已完成 |
| 04 | 上下文管理与压缩 | `04-上下文管理与压缩四项目对比分析.md` | 982 | 3 | L4 | 已完成 |
| 05 | 消息 / 事件 / Session | `05-消息事件与Session结构四项目对比分析.md` | 630 | 2 | L5 | 已完成 |
| 06 | 会话持久化与恢复 | `06-会话持久化与恢复四项目对比分析.md` | 738 | 3 | L6 | 已完成 |
| 07 | 任务与子 Agent | `07-任务与子Agent四项目对比分析.md` | 1102 | 4 | L7 | 已完成 |
| 08 | 权限与沙箱 | `08-权限与沙箱四项目对比分析.md` | 1046 | 5 | L8 | 已完成 |
| 09 | 模型调用与 Provider | `09-模型调用与Provider四项目对比分析.md` | 1393 | 5 | L9 | 已完成 |
| 10 | Hook 与扩展机制 | `10-Hook与扩展机制四项目对比分析.md` | 1014 | 4 | L10 | 已完成 |
| 13 | MCP 与外部能力接入 | `13-MCP与外部能力接入四项目对比分析.md` | 2080 | 3 | L11 | 已完成 |
| 14 | 长时任务与后台作业 | `14-长时任务与后台作业四项目对比分析.md` | 3907 | 2 | L12 | 已完成 |
| 15 | 对外接口与集成协议 | `15-对外接口与集成协议四项目对比分析.md` | 2708 | 3 | L13 | 已完成 |
| 16 | 交互层与终端 UI 机制 | `16-交互层与终端UI机制四项目对比分析.md` | 2008 | 3 | L14 | 已完成 |
| 17 | 遥测 · 追踪 · 诊断 | `17-遥测追踪与诊断四项目对比分析.md` | — | — | L15 | 规划 |

> 表格按**层次槽位**排序（启动区 → 内核区 → 外围区），不是按章编号排序——两者在内核区与外围区之间是错位的。

---

## 五、阅读路径建议

**路径 A｜快速建立整体认知（约 30 分钟）**
`00 本文` → 各篇「一、核心结论速览」→ 各篇「五、横向对比矩阵」→ 跳到「七、设计建议」

**路径 B｜深度研读某一层**
`00 本文 三、术语对照表` → 目标篇全文（按 一 → 七 顺序，四、逐项目实现是细节来源）→ 对照「附录：关键文件索引」打开源码验证

**路径 C｜落地选型（要自己写一个 Agent）**
`00 本文 二、模块地图` → 08 权限与沙箱 → 01 主循环 → 04 上下文压缩 → 06 持久化 → 07 任务与子 Agent → 各篇 七、设计建议合并成选型清单

**路径 D｜从零搭一个（按运行时发生顺序读）**
按三区顺序走一遍**实际执行链**：11 配置 · 凭据与启动引导 → 12 提示词与上下文注入 → 01 主循环 → 03 工具定义 → 02 工具调用 → 04 上下文压缩 → 06 持久化 → 09 模型调用与 Provider → 08 权限与沙箱。这条路径回答的是「一次请求从进程启动到拿到回答，依次经过哪些层」，与路径 C 按重要性排列不同。

**路径 E｜要把它嵌进产品（做 IDE 插件 / Web UI / 自动化编排）**
`00 本文 二、模块地图` 外围区 → 15 对外接口与集成协议 → 16 交互层与终端 UI 机制 → 13 MCP 与外部能力接入 → 12 提示词与上下文注入 → 17 遥测 · 追踪 · 诊断 → 14 长时任务与后台作业。这条路径关心的是「它怎么被别的程序驱动、怎么被看见、怎么被观测」，而非它内部怎么跑。

---

## 六、证据等级约定

所有结论统一标注证据强度：

| 标记 | 含义 |
|---|---|
| `[代码]` | 源码中可直接读到的实现，附 `文件:行号` |
| `[门控]` | 被 `feature('XXX')` / `USER_TYPE` 编译开关包裹，**外部发布版中被裁剪**；还原版里的开关值是硬编码的，不能当作生产行为 |
| `[推断]` | 从调用关系、命名、注释反推的意图，无直接代码断言 |
| `[注释]` | 源码注释/提交信息中给出的**设计理由**（本系列最看重的一类证据） |

**Claude-Code 专属提示**：该项目大量能力由 `feature()` 门控（例如 `feature('REACTIVE_COMPACT')`、`feature('CONTEXT_COLLAPSE')`，见 `src/query.ts:15-21`）。引用其实现时一律标 `[门控]`，并说明「还原版可见 ≠ 发布版启用」。

---

## 七、文档规范（各篇统一骨架）

为保证「模块 / 层次 / 逻辑」三者清晰，**全部已产出章节（内核 01–10、启动区 11–12、外围区 13–17）**均按以下 **7 段骨架**产出，**章节名与顺序固定**，且**一问对应一节**，读者可跨篇按问题跳读。骨架与三区、章编号无关，启动区与外围区各章同构：

| 段 | 章节名 | 回答的问题 | 硬要求 |
|---|---|---|---|
| 一 | **核心结论速览** | Q1–Q6 的答案压缩（不是独立一问） | ≤5 条，每条一行 |
| 二 | **本层职责与边界** | **Q1** 这一层是什么？管什么、不管什么？上下游是谁？ | 配 1 张层次定位图 |
| 三 | **概念对齐表** | **Q2** 同一个概念，四家分别叫什么？ | 表格，空白格必须保留 |
| 四 | **逐项目实现** | **Q3** 四家各自的核心流程是什么？入口 → 主流程 → 状态载体怎么走？ | 每个断言附 `文件:行号` |
| 五 | **横向对比矩阵** | **Q4** 同一维度上四家分别怎么做？共识度多高？ | 禁止「按项目分段叙述」，必附共识度列 |
| 六 | **异常与降级** | **Q5** 出错、越界、被中断时四家怎么办？为什么这么办？ | 每条必须给依据，不给依据的不写 |
| 七 | **设计建议** | **Q6** 哪些能直接采纳？哪些要按产品形态权衡？哪些明确不该做？ | 分为「共识 / 推荐 / 权衡 / 反例」四级 |
| 附 | **关键文件索引** | **Q7** 这些结论在源码哪里？怎么复核？ | 表格：项目 / 文件 / 行号 / 用途 |

> **「边界」有两个意思，务必分清**：第二段的「边界」指**职责范围**（这层管什么、不管什么），第六段已正名为**「异常与降级」**（出错、越界、被中断时的行为）。两义不可混读。
>
> **「共识度」是 Q4 通往 Q6 的桥梁**：四家一致 → 进第七段「共识」级；只有一家这么做 → 进「权衡」级。因此第七段每条建议都能回溯到第五段某个矩阵行，而不是主观意见。

### 7.1 排版约定（全系列统一）

| 元素 | 写法 | 示例 |
|---|---|---|
| 章标题 | `# 第 N 章：主题 —— 一句副标题` | `# 第 4 章：上下文管理与压缩 —— 窗口装不下时，对话怎么办` |
| 导语段 | 章标题之后、元信息块之前，一段**纯文字** | 说明「本章回答什么问题、读完能得到什么」 |
| 元信息块 | 引用块，紧接导语段 | `> **本层定位**：L4，……` |
| 一级分节 | **中文数字 + 顿号**（固定七段） | `## 五、横向对比矩阵` |
| 二级分节 | 阿拉伯数字，序号跟随一级 | `### 5.4 安装（写回会话）` |
| 三级分节 | 阿拉伯数字，序号跟随二级 | `#### 4.4.3 主流程：compactConversation` |
| 附录 | 不参与编号 | `## 附录：关键文件索引` |
| 图示 | 结构/层次 → **盒线字符图**；流程/时序/状态 → **Mermaid** | 见各篇「二、本层职责与边界」 |
| 图注 | 紧跟图块之后，格式 `**图 N-M**：` | `**图 4-2**：四家策略层数的阶梯。……` |
| 边注 / 提示 | 引用块 + 加粗引导词 | `> **说明**：……` / `> **设计理由**：……` |
| 小节分隔 | `---` | 一级分节之间 |
| 引用写法 | 跨章 → `见第 N 章`；章内小节 → `见 N.M`；文档自称 → 「本篇」 | 「见第 6 章」「见 5.6」 |

**为什么图示分两类**：盒线字符图不依赖渲染器，在任何 Markdown 阅读器里都能看；Mermaid 表现力更强但需要渲染支持。因此**每篇至少配 1 张盒线图作为兜底**，流程类内容再叠加 Mermaid。

**为什么不用 `§` 编号**：`§` 是英文语境下的节号，与中文数字分节混用会同时损失可读性与检索一致性。全系列统一后，正文里任何一处 `见 5.6` 都能直接对应到标题 `### 5.6`。

---

## 八、跨项目源码锚点速查

| 关注点 | pi | deepseek-harness | codex | Claude-Code |
|---|---|---|---|---|
| 配置分层与优先级 | `core/settings-manager.ts:350` `deepMergeSettings` | `app-boot/src/profile-context.ts:65` `readProfilePatches` | `config/src/config_layer_source.rs:33-51` `precedence()` | `utils/settings/constants.ts:7-22` `SETTING_SOURCES` |
| 配置 schema 与未知键 | `core/model-config.ts:242-245`（仅 `models.json` 上 typebox） | `vendor/schemastery/src/index.ts:812-823`（非 strict 时 merge 回去） | `config/src/config_toml.rs:163-166`（`deny_unknown_fields`） | `utils/settings/types.ts:209-215`（`.passthrough()`） |
| 环境变量这一档 | `core/settings-manager.ts:1235-1241`（个别 getter 兜底） | `boot/app-boot/src/index.ts:225-246` `loadLayeredEnv` | `codex_home_symlink.rs:23`（唯一环境变量 `CODEX_HOME`） | `utils/managedEnv.ts:124-178` `applySafeConfigEnvironmentVariables` |
| 凭据解析入口 | `ai/src/auth/resolve.ts:73-109` | `credentials-local/src/index.ts:609-620` | `login/src/auth/manager.rs:1483-1493` | `utils/auth.ts:153-206` |
| 凭据存储与权限 | `core/auth-storage.ts:24-25`（`0600` / 目录 `0700`） | `credentials-local/src/index.ts:127-146`（启动时复检） | `login/src/auth/storage.rs:430-457`（keyring 回退） | `utils/secureStorage/plainTextStorage.ts:44-69` |
| 启动引导序列 | `coding-agent/src/main.ts:584-588`、`:735`（设置管理器两次创建） | `boot/app-boot/src/index.ts:938-997`（`stage` 分段） | `tui/src/startup_orchestration.rs:1-5`（配置与 UI 并行） | `entrypoints/init.ts:57-238` |
| 配置演化与项目信任 | `coding-agent/src/migrations.ts:305-315` + `core/project-trust.ts:86-88` | `credentials-local/src/index.ts:205-220`（无项目信任） | `config/src/key_aliases.rs:11-37` + `config/src/loader/mod.rs:80-97` | `main.tsx:323-352` + `utils/config.ts:689-743` |
| 主循环入口 | `agent-loop.ts` | `agent-loop/src/agent.ts` | `session/turn.rs` | `query.ts` / `QueryEngine.ts` |
| 主循环状态机 | `runLoop` 双层 while | `turn()` / `step()` | `run_turn` loop | `submitMessage` async generator |
| 停止钩子 | `finishTurn` 决策 | `turn-stopping` 瀑布 | stop hooks | `query/stopHooks.ts` |
| token 预算 | 无内置 | 无内置 | `compact_token_budget.rs` | `query/tokenBudget.ts` |
| 压缩入口 | `compaction/compaction.ts` | `compaction/src/index.ts` | `compact.rs` | `services/compact/autoCompact.ts` |
| 工具注册表 | `harness/tools/index.ts` | `core/tools/src/index.ts` | `core/src/tools/registry.rs` | `tools.ts` |
| 会话类型定义 | `harness/session/types.ts` | `core/session/src/types.ts` | `protocol/src/items.rs` | `types/message.ts` |
| 持久化读写 | `session/jsonl/{io,storage}.ts` | `session-persistence-jsonl/src/index.ts` | `rollout/src/recorder.rs` | `utils/sessionStoragePortable.ts` |
| 子 Agent 委派入口 | 扩展层 `examples/extensions/subagent` | `subagent/tool-subagent/src/index.ts` | `core/src/agent/` + `multi_agents_spec.rs` | `tools/AgentTool/AgentTool.tsx` |
| 子 Agent 深度限制 | 无（示例未设限） | `subagent/src/depth.ts` | `core/src/agent/registry.rs` | `constants/tools.ts` |
| 任务/子 Agent 实体 | 无（独立进程） | `SubagentRun`（`subagent/src/types.ts`） | `ThreadId`（`agent-graph-store`） | `src/Task.ts` |
| 权限判定入口 | `beforeToolCall`（`agent/src/types.ts:66`） | `sandbox/resolve`（`sandbox-policy/src/index.ts:164`） | `orchestrator.rs:122` | `hasPermissionsToUseTool`（`permissions.ts:473`） |
| 沙箱模式定义 | — | `SandboxMode`（`sandbox/src/index.ts:29`） | `SandboxPolicy`（`protocol.rs:1072`） | 并入 `PermissionMode`（`types/permissions.ts:16`） |
| 规则/策略载体 | 扩展代码 | `bundle/base/cordis.patch.yml:228` | `<codex_home>/rules/*.rules` | settings 文件（8 类来源） |
| 系统隔离实现 | 无（README.md:40） | `sandbox-local/src/profiles.ts` | `sandboxing/src/{seatbelt,landlock}.rs` | `sandbox-adapter.ts:18`（外部包） |
| 子 Agent 权限继承 | — | `subagent/src/child-agent.ts:246` | `tools/handlers/multi_agents.rs:5` | `AgentTool/runAgent.ts:421` |
| 扩展挂载点全集 | `extensions/types.ts`（36 事件） | `runtime-types.ts:248-381`（7 拦截点） | `protocol.rs:1577`（12 事件） | `sdk/coreTypes.ts:25`（27 事件） |
| 扩展加载入口 | `extensions/loader.ts:749` | `bundle/base/cordis.patch.yml` | `core-plugins/src/loader.rs` | `utils/plugins/pluginLoader.ts` |
| 钩子执行器 | `extensions/runner.ts` | `hook-protocol/src/runner.ts` | `hooks/src/engine/dispatcher.rs` | `utils/hooks.ts` |
| 模型调用入口 | `agent-loop.ts:380` `streamAssistantResponse` | `agent-loop/src/agent.ts:529` `prepareRequest` | `session/turn.rs:1584` `run_sampling_request` | `query.ts:659` `deps.callModel` |
| provider 注册表 | `ai/src/providers/all.ts`（41 家） | `llm/src/index.ts:337` `LlmRuntime` | `model-provider-info/src/lib.rs:656` | `utils/model/providers.ts:6` |
| 流式适配层 | `ai/src/api/*.ts`（20 模块） | `{llm-deepseek,llm-pi-ai}/src/adapter.ts` | `codex-api/src/sse/responses.rs`（唯一消化点） | `services/api/claude.ts:1940` |
| 统一事件词汇表 | `ai/src/types.ts:652`（12 种） | `llm/src/types.ts:440`（7 种） | `codex-api/src/common.rs:80`（17 种） | 无（原生 `part.type`） |
| 重试策略入口 | `ai/src/utils/provider-retry.ts:109`（默认 0） | `llm/src/retry-policy.ts:14`（5 次） | `codex-client/src/retry.rs:41`（4 / 5 次） | `services/api/withRetry.ts:52`（10 次） |
| token 计量 | `ai/src/models.ts:900` `calculateCost` | `token-meter/src/index.ts:101`（日志回放） | `protocol/src/protocol.rs:2235` | `services/api/claude.ts:2924` |
| 凭据解析 | `ai/src/auth/resolve.ts:63` | `credentials-local/src/index.ts:609` | `model-provider/src/auth.rs:201` | `services/api/client.ts:302` |
| 系统提示词组装入口 | `core/system-prompt.ts:121` `buildSystemPromptSections`（渲染 `:195`） | `core/system-prompt/src/index.ts:279` `renderPrompt`（顺序表 `:125`） | `session/world_state.rs:34` `build_world_state_for_step` | `constants/prompts.ts:444` `getSystemPrompt` |
| 分节重算与差分 | `agent-session.ts:1407` `diffSystemPromptSections` | `runtime-context.ts:95` append / replace 投影 | `session/mod.rs:3643` 差分（全量 `:4554`） | `systemPromptSections.ts:50` 段缓存 |
| 项目指令发现 | `core/resource-loader.ts:71`（候选表 `:72`） | `context/agent-instructions/src/files.ts:94` | `core/src/agents_md.rs:64`（截断 `:156`） | `utils/claudemd.ts:402` |
| 项目指令预算 | 无 | 单文件 1 MiB（`agent-instructions/src/config.ts:14`） | 32 KiB（`core/src/config/mod.rs:250`） | 40,000 字符（`utils/claudemd.ts:92`） |
| 环境探测渲染 | `core/system-prompt.ts:170`（仅 cwd） | `context/time-context/src/index.ts:100` | `context/world_state/environment.rs:29` | `constants/prompts.ts:677` `computeSimpleEnvInfo` |
| 缓存边界 / 前缀治理 | 分节自定界（`ai/src/types.ts:494`） | `interaction/user-approval/src/index.ts:160` | `client.rs:899` 稳定 ID + cache key `:571` | `constants/prompts.ts:105` 边界标记 → `services/api/claude.ts:3228` |
| 用户上下文（并行注入） | — | runtime-context 快照 | `session/mod.rs:4168` 初始片段 | `src/context.ts:96` git 状态 / `:186` 日期 |
| 进程创建与 PTY | `tools/bash.ts:98`（`detached`） | `subprocess-local/src/spawn.ts:461` | `core/src/spawn.rs:99`（`pre_exec`） | `utils/Shell.ts:316`（`stdio` / `detached`） |
| 回收整棵进程树 | `utils/shell.ts:216`（负 pid `kill`） | `spawn.ts:97`（`killGroup`） | `utils/pty/src/process_group.rs:92`（`killpg`） | `ShellCommand.ts:340`（`treeKill`） |
| 程序化工具调用 | —（无通道） | `core/tools/src/ptc.ts:30`（`run_code`） | `code-mode-protocol/src/lib.rs:51`（`exec` / `wait`） | `REPLTool/constants.ts:11`（`REPL`） |
| 对等协作的载体 | `harness/pico3/harness.ts:329`（子树校验） | `agent-team/src/task-board.ts:119`（版本号 CAS） | `ext/agent-message-board/src/local.rs:55`（SQLite 消息板） | `utils/teammateMailbox.ts:4`（文件邮箱） |
| MCP 接入入口 | 无（`coding-agent/README.md:537` 声明 No MCP） | `mcp/mcp-client/src/index.ts:154` `apply` | `codex-rs/codex-mcp/src/connection_manager.rs` | `services/mcp/client.ts:595` `connectToServer` |
| MCP 配置载体与来源仲裁 | 无 | Cordis 插件 `config`，一实例一 server（`mcp-client/src/index.ts:119`） | `config.toml` `[mcp_servers]` + 5 类来源（`config_toml.rs:290`、`catalog.rs:70`） | `.mcp.json` 等 7 种 scope + 企业独占（`mcp/types.ts:10`、`mcp/config.ts:1082`） |
| MCP 命名空间与长度上限 | 无 | `mcp-client/src/tools.ts:48`（64） | `codex-mcp/src/mcp/mod.rs:83` + `tools.rs:226`（128） | `mcp/mcpStringUtils.ts:39-52`（无名字上限） |
| MCP 审批落点 | 无 | 统一注册表 `ctx.tools.register`（`mcp-client/src/tools.ts:150`） | 一等动作 `ApprovalAction::McpToolCall`（`core/src/mcp_tool_call.rs:1536`） | 统一规则匹配（`utils/permissions/permissions.ts:247-268`） |
| MCP 凭据 | 无 | 静态 `env` / `headers`（`mcp-client/src/transport.ts:37`） | keyring + OAuth + 企业 EMA（`oauth.rs:92`、`mcp_ema.rs:29`） | SecureStorage + OAuth + XAA（`mcp/auth.ts:1376`、`mcp/xaa.ts:1-17`） |
| MCP 反向能力 | 无 | 只做 resources（`mcp-resources/src/tools.ts:33-41`） | elicitation 三选一（`codex-mcp/src/elicitation.rs:1-7`） | roots + elicitation（`mcp/client.ts:994-1000`） |
| 后台作业的实体与登记 | `harness/pico3/harness.ts:156`（六个内建 kind 含 `job`）+ `harness/pico3/session.ts:310`（`liveTasks`） | `jobs/jobs-local/src/index.ts:160`（`JobRegistry.store` 内存表） | `codex-rs/core/src/unified_exec/process_manager.rs:1816-1833`（内核进程表） | `src/tasks.ts:22-39`（`getAllTasks`）+ `src/Task.ts:6-20`（7 型联合 / 5 态） |
| 长时作业的触发与调度 | `harness/pico3/kinds/job.ts:7-8`（`every` / `notBefore`） | `schedule/schedule/src/domain.ts:24-25`（`every` ≥ 300 秒） | 无内建调度器（只有一次性 `clock.sleep`） | `utils/cronScheduler.ts:48`（1 秒 tick）+ `utils/cronTasks.ts:74`（落盘路径） |
| 长时作业输出的回注 | `harness/pico3/session.ts:729-733`（`pi.notice` 强制恰一条 user 消息） | `jobs/tool-jobs/src/index.ts:292-298`（空闲 `followup` / 忙 `inject`） | 纯工具返回 `process/read` + `ExecCommandEnd` | `utils/task/framework.ts:283-290`（`<task-notification>` attachment） |
| 长时作业的模型可达提交面 | **无**（`kinds.job` 非测试引用为零） | `job_output` / `job_list` / `job_kill` + 各 kind 提交工具 | `process/*` 8 条按 id，**协议里无列举**（`codex-rs/exec-server-protocol/src/protocol.rs:24-31`） | `Bash` 的 `run_in_background` + `TaskOutput` / `TaskStop` |
| 跨进程重启的让位点 | 取决于注入的 `ProcessHost`（进程句柄写进检查点） | 作业纯内存；定时靠会话事件 + 持久化屏障 | `attach` 重挂（30 秒窗口，`codex-rs/exec-server/src/server/session_registry.rs:20`） | 仅 `RemoteAgentTask` 有 sidecar |
| 长时作业的取消与回收 | `harness/pico3/scheduler.ts:373`（`abortTask` 的相位降级） | `JobRegistry.kill`（先 cancel 再置 `stopping`） | `process/terminate` + `termination_requested` 标记 | `src/tasks/stopTask.ts:57-95`（查表 → 校验 running → `kill`） |
| 长时作业的并发与配额 | 无上限 | 每 owner 10（`jobs-local` 的 preflight） | 内核软上限 64（`codex-rs/core/src/unified_exec/mod.rs:82`，LRU 淘汰） | 无通用上限；cron 为 50（`CronCreateTool.ts:25`） |
| 对外驱动面入口 | `server/src/server.ts:46`（Unix socket 主栈）/ `modes/rpc/rpc-mode.ts:54`（stdio headless） | `acp/acp/src/index.ts:102` / `sdk/server/src/server.ts:81` / `api/gateway/src/stream-server.ts:44` / `webhook/webhook/src/index.ts:141` | `app-server-transport/src/transport/mod.rs:83-88`（四值承载） | `cli/structuredIO.ts:135`（`--print` 管道）/ `bridge/bridgeEnabled.ts:28` / `entrypoints/mcp.ts:35` |
| 对外协议定义与宽度 | `protocol/src/protocol.ts:62` / `:98`（两套 envelope，7 服务 24 成员） | `sdk/protocol/src/types.ts:115-119`（3 条请求） | `app-server-protocol/src/protocol/common.rs:498`（`ClientRequest` 170 条）/ `:1916`（84 条通知） | `entrypoints/sdk/controlSchemas.ts:552-576`（21 子类型）/ `coreSchemas.ts:1854-1881`（24 种消息） |
| 分帧与编码 | `protocol/src/framing.ts:1-6`（4 字节长度前缀 + CBOR） | `sdk/protocol/src/transport.ts`（各承载自带） | `app-server-transport/src/transport/stdio.rs`（NDJSON） | `cli/structuredIO.ts:465-467`（`ndjsonSafeStringify`） |
| 握手与协议版本 | `protocol/src/codec.ts:139-141`（等值校验，`PROTOCOL_VERSION = 8`） | `acp/acp/src/index.ts:176-194`（capabilities 协商） | `app-server-protocol/src/protocol/v1.rs`（`InitializeCapabilities` 六项） | `cli/print.ts:4355-4365`（重复初始化回 `Already initialized`） |
| 会话镜像与附着 | `server/src/session-router.ts:20-24`（`attachments` 是 `Set`） | `acp/acp/src/index.ts:243-246`（第二个持有者被拒） | `app-server/src/thread_state.rs:316-320`（`connection_ids` 是 `HashSet`） | `remote/RemoteSessionManager.ts:56-61`（`viewerOnly` 只读） |
| 审批回流通道 | 无（`server/transports/unix/types.ts:5` 仅一条 socket 权限注释） | `acp/acp/src/index.ts:155-172`（2 档 once） | `app-server-protocol/.../v2/item.rs:66-85`（6 档）/ `bespoke_event_handling.rs:1979-1981`（逐档翻译） | `cli/structuredIO.ts:590-606`（`can_use_tool`）/ `bridge/bridgePermissionCallbacks.ts:3-8`（二值） |
| 对外鉴权与承载绑定 | `coding-agent/src/experimental/server.ts:471-476`（socket `0o600`） | `client/connection/src/browser-auth.ts:125-132`（Cookie + token） | `app-server-transport/src/transport/auth.rs:266-271`（非 loopback 无凭证拒启） | `bridge/trustedDevice.ts:15-33`（`X-Trusted-Device-Token`） |
| 反向当 server（对外提供工具） | 无 | 无 | 无 | `entrypoints/mcp.ts:35`（`claude mcp serve`） |
| 帧调度与合帧 | `packages/tui/src/tui.ts:952-961` `requestRender` | `packages/client/store/src/index.ts:71-88` `rafBatch` | `codex-rs/tui/src/tui/frame_requester.rs:96-127` `run` | `src/ink/ink.tsx:213-216` `throttle` |
| 差量比对对象 | `packages/tui/src/tui-main-screen.ts:362-378`（行数组） | —（交给虚拟 DOM） | `codex-rs/tui/src/custom_terminal.rs:444-460` `diff_buffers` | `src/ink/ink.tsx:586-591` `diff`（Screen 单元） |
| 流式增量的提交边界 | `packages/coding-agent/src/modes/interactive/components/assistant-message.ts:91-201` `updateContent`（无边界） | `packages/client/ui-chat/src/client/conversation-nodes/assistant.ts:316-321`（发布节奏声明） | `codex-rs/tui/src/streaming/commit_tick.rs:180-188` `DrainPlan`（换行边界） | `src/screens/REPL.tsx:1505`（最后一个换行） |
| 工具行状态载体 | `packages/coding-agent/src/modes/interactive/components/tool-execution.ts:188-198` | `packages/client/ui-tool/src/client/tool/models/tool-call-model.ts:237-270` `toolRowModel`（纯函数） | `codex-rs/tui/src/chatwidget/interrupts.rs:48-57`（审批队列） | `src/components/messages/AssistantToolUseMessage.tsx:112-121` |
| 中断键位与双按退出 | `packages/coding-agent/src/modes/interactive/interactive-mode.ts:2965-2991`（Esc）/ `:4116-4129`（两按） | `packages/client/ui-conversation/src/client/skeleton/InputBar.tsx:302-321`（按钮角色切换） | `codex-rs/tui/src/bottom_pane/mod.rs:229-233`（双按退出**关闭**）/ `keymap.rs:1662` | `src/keybindings/defaultBindings.ts:36-44` / `:67` |
| 终端能力探测 | `packages/tui/src/terminal-image.ts:128-132`（未知终端保守降级） | 不适用（浏览器宿主） | `codex-rs/terminal-detection/src/lib.rs:1-5`（只读环境变量，禁执行外部命令） | `src/ink/colorize.ts:47-57`（tmux 主动压档 + 逃生口） |
| 已完成内容的归宿 | 留在组件树，随视口滚动 | DOM + 语义锚点补偿（`packages/client/ui-chat/src/client/chat/use-chat-viewport.ts:367-372`） | `codex-rs/tui/src/insert_history.rs:204-217`（写进终端原生 scrollback） | 留在 `Screen` 与终端屏里 |
| 视口虚拟化 | 不做（裁剪在绘制期，`packages/tui/src/layout.ts:330-377`） | 会话流不做，轨迹视图做（`packages/client/ui-trajectory/src/client/trajectory-virtual-rows.ts:51-83`） | 不做（已完成内容交还终端） | 不做（整屏 `Screen` 单元比对） |

---

*编号规则：`NN-主题.md`。**章编号表示撰写批次，层次槽位（`L0.1` / `L1`–`L15`）表示架构位置**——两者在内核区与外围区之间错位，以第二节模块地图为准。术语、锚点与规范以本文件与各章正文为准。*
